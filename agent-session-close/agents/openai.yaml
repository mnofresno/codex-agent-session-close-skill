interface:
  display_name: "Agent Session Close"
  short_description: "Close agents safely without touching others"
  default_prompt: "Use $agent-session-close to finish the requested task, then force-close the current Terminal window when supported, without touching unrelated agents."
